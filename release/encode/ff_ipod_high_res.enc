# Description (single line, keep short)
<description>
mpeg4 container, res x=640 30fps h.264 video, 48KHz/128Kbps 2 chan aac audio

# Encode command or script (single line)
# Known keywords: FFMPEG, HANDBRAKE, MENCODER, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
FFMPEG -y -i INPUT -threads CPU_CORES -vcodec libx264 -level 30 -subq 6 -me_range 16 -qmin 15 -qmax 20 -g 300 -s 640xHEIGHT -r 29.97 -b 1500k -maxrate 2000k -acodec aac -dts_delta_threshold 1 -async 48000 -ac 2 -ab 128k -ar 48000 -f mp4 OUTPUT

# Encoded output file extension
<extension>
mp4
