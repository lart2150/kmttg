# Description (single line, keep short)
<description>
mpeg4 container, same res 5 Mbps h.264 video, copy audio (ac3)

# Encode command or script (single line)
# Known keywords: FFMPEG, HANDBRAKE, MENCODER, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
FFMPEG -y -i INPUT -threads CPU_CORES -acodec copy -vcodec libx264 -level 41 -b 5000k -refs 3 -flags2 +mixed_refs+wpred+bpyramid+dct8x8-fastpskip -bf 3 -me_method umh -subq 9 -me_range 16 -qmin 10 -qmax 50 -g 24 -keyint_min 2 -f mp4 OUTPUT

# Encoded output file extension
<extension>
mp4
