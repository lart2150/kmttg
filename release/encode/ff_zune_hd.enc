# Description (single line, keep short)
<description>
Zune HD: mpeg4 container, 480x272, mp4 video, aac audio

# Encode command or script (single line)
# Known keywords: FFMPEG, HANDBRAKE, MENCODER, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
FFMPEG -y -i INPUT -threads CPU_CORES -f mp4 -vcodec mpeg4 -b 1000k -r 23.976 -s 480x272 -acodec aac -dts_delta_threshold 1 -async 48000 -ab 128k -ar 48000 -ac 2 OUTPUT

# Encoded output file extension
<extension>
mp4
