# Description (single line, keep short)
<description>
Handbrake film: mkv container, same res h.264 video, ac3 audio

# Encode command or script (single line)
# Known keywords: FFMPEG, MENCODER, HANDBRAKE, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
HANDBRAKE -i INPUT -t 1 -c 1 -f mkv --large-file -e x264 -b 1800 -2  -T  -a 1 -E ac3 -B 160 -6 dpl2 -D 1 -x threads=CPU_CORES:ref=3:mixed-refs=1:bframes=6:weightb=1:direct=auto:b-pyramid=1:me=umh:subq=9:analyse=all:8x8dct=1:trellis=1:no-fast-pskip=1:psy-rd=1,1 -v -o OUTPUT
 
# Encoded output file extension
<extension>
mkv
