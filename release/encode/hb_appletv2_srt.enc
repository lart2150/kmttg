# Description (single line, keep short)
<description>
Handbrake AppleTV: m4v container, AppleTV 2 preset (h.264 video, aac & ac3 audio & captions)

# Encode command or script (single line)
# Known keywords: FFMPEG, MENCODER, HANDBRAKE, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
HANDBRAKE -i INPUT --preset "AppleTV 2" -f m4v -D 1,1 -A "Stereo","AC3" --rate 29.97 --srt-file SRTFILE --srt-codeset UTF-8 --srt-lang eng --srt-default -v -o OUTPUT

# Encoded output file extension
<extension>
m4v
