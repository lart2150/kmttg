# Description (single line, keep short)
<description>
Motorola Droid: mp4 container, 854x480 mpeg4 video, AAC audio

# Encode command or script (single line)
# Known keywords: FFMPEG, HANDBRAKE, MENCODER, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
FFMPEG -y -i INPUT -threads CPU_CORES -vcodec libx264 -acodec aac -strict -2 -ac 2 -s 854x480 -coder 0 -level 41 -subq 6 -me_range 16 -qmin 10 -qmax 50 -g 300 -bufsize 14745k -b 2000k -maxrate 16000k -me_method epzs -trellis 2 -mbd 1 -f mp4 OUTPUT

# Encoded output file extension
<extension>
mp4
