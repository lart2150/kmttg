# Description (single line, keep short)
<description>
mpeg4 container, constant rate factor h.264 video, copy audio (ac3)

# Encode command or script (single line)
# Known keywords: FFMPEG, HANDBRAKE, MENCODER, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
FFMPEG -y -i INPUT -threads CPU_CORES -vcodec libx264 -preset slow -crf 20 -level 41 -refs 3 -acodec copy -f mp4 OUTPUT

# Encoded output file extension
<extension>
mp4
