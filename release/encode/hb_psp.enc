# Description (single line, keep short)
<description>
Handbrake PSP: m4v container, res x=368 h.264 video, aac audio

# Encode command or script (single line)
# Known keywords: FFMPEG, MENCODER, HANDBRAKE, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
HANDBRAKE -i INPUT -t 1 -c 1 -f m4v -X 368 -Y 208 -e ffmpeg -b 1024 -a 1 -E faac -B 128 -R 48 -6 dpl2 -D 1 -v -o OUTPUT
 
# Encoded output file extension
<extension>
m4v
