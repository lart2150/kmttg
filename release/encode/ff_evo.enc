# Description (single line, keep short)
<description>
HTC Evo: mp4 container, 800x480 mpeg4 video, AAC audio
# http://martin-fleming.co.uk/2011/02/converting-videos-for-android-using-command-line-ffmpeg-in-mpeg4-or-h264/
# http://darrenjw.posterous.com/creating-video-for-android-phones-from-ubuntu
# Encode command or script (single line)
# Known keywords: FFMPEG, HANDBRAKE, MENCODER, PERL, INPUT, OUTPUT, PWD, CPU_CORES, SRTFILE
<command>
FFMPEG -y -i INPUT -s 800x480 -vcodec libx264 -r 23.976 -flags +loop+mv4 -cmp 256 -partitions +parti4x4+parti8x8+partp4x4+partp8x8+partb8x8 -subq 7 -trellis 1 -refs 5 -bf 0 -coder 0 -me_range 16 -g 250 -keyint_min 25 -sc_threshold 40 -i_qfactor 0.71 -qmin 10 -qmax 25 -qdiff 4 -acodec aac -strict -2 -ac 2 -ar 48000 -ab 192k -vol 2560 -f mp4 OUTPUT
# Encoded output file extension
<extension>
mp4
